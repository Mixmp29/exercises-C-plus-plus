/*
  Упражнение 2.22. С учетом того, что p является указателем на тип int,
  объясните следующий код:

  if (p) // Если указателю p присвоен какой-либо адрес, то условие Истина.
         // Если указатель p инициализирован nullptr или 0, то Ложь.
         // (в указателе нет никакого адреса).

  if (*p) // Если значение переменной, расположенной по адресу, указанном в p,
          // не является 0, то Истина, иначе Ложь
 */