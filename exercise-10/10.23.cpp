// Упражнение 10.23. Сколько аргументов получает функция bind()?

// Столько же сколько аргументов функции, которую она будет вызывать + 1 для
// имени этой функции в начале.

// void f(names){}

// auto bf = bind(f, names);